# Pipeline (chesspipe) dependencies. Prefer `pip install -e .` which reads
# pyproject.toml; this file mirrors those deps for environments that want it.
python-chess
python-dotenv
requests
psycopg[binary]
python-telegram-bot
cairosvg
